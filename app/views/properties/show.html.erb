<p id="notice"><%= notice %></p>
      <%= image_tag @property.property_cover_photo if @property.property_cover_photo.present? %>
<br>
<br>
<%= @property.user.email %>
<% if @property.for_sale? %>
<%= @property.price > 0 ? number_to_currency(@property.price, precision: 0) : "for sale" %>
<% else %>
<%= @property.price > 0 ? number_to_currency(@property.price, precision: 0) : "for sale" %>/month
<% end %>
<%= @property.address %>

<%= @property.bedrooms %>
<br>
<br>
<%= @property.user.company_name %>
<br>
<%= @property.user.country_code %>
<%= @property.user.phone_number %>
<br>

nearby properties
<br>
<div class="flex space-x-4 px-24">
<% if @property.nearbys.available.present? %>
    <% @property.nearbys.available.last(4).each do |property| %>
        <%= link_to property do %>
            <%= image_tag property.property_cover_photo , class: "h-32 object-cover object-center w-40"%>
            (<%= property.distance.round(2) %> kms)
        <% end %>
        <br>
    <% end %>
<% end %>
</div>
<div class="">
<%= render 'agent_properties' %>
</div>
<%= link_to 'Edit', edit_property_path(@property) %> |
<%= link_to 'Back', properties_path %>