<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.company_name, user_path(user) %></li>
            <li>
            <% if user.approved %>
              approved
              <% else %>
              not approved
            <% end %>
            </li>

  <% end %>
</ul>
