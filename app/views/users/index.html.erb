<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.email, user_path(user) %></li>
            <li>
            <% if user.approved %>
              approved
              <% else %>
              not approved
            <% end %>
            </li>

  <% end %>
</ul>
